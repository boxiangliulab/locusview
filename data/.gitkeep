# This file keeps the (otherwise gitignored) data/ directory in version control.
# Real QTL data is never committed — it can be many gigabytes. See .gitignore.
# Small, curated test fixtures live under data/fixtures/ and ARE tracked.
